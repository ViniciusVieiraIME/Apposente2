#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createTransactionUpdatesInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createTransactionUpdatesInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO transactionUpdates $colStr VALUES $valStr", "SELECT * FROM transactionUpdates WHERE transactionupdateid=$ctx.args.createTransactionUpdatesInput.transactionupdateid"]
}