#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createAvgPriceTInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createAvgPriceTInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO avgPriceT $colStr VALUES $valStr", "SELECT * FROM avgPriceT WHERE avgPriceID=$ctx.args.createAvgPriceTInput.avgPriceID"]
}