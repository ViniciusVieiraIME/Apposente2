#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createCurrentpositionInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createCurrentpositionInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO currentposition $colStr VALUES $valStr", "SELECT * FROM currentposition WHERE currentpositionid=$ctx.args.createCurrentpositionInput.currentpositionid"]
}